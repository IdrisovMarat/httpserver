# 🐦 Chirpy API

RESTful API для социальной платформы микроблогов Chirpy. Позволяет пользователям создавать, читать, обновлять и удалять короткие сообщения (chirps), а также управлять учетными записями и подписками.

## 🚀 Особенности

- **🔐 Аутентификация** - JWT токены с refresh/access системой
- **🐦 Chirps** - Создание и управление короткими сообщениями
- **👥 Пользователи** - Регистрация, аутентификация, управление профилем
- **💎 Chirpy Red** - Система премиум-подписок через Polka
- **📊 Администрирование** - Мониторинг и управление сервером
- **🛡️ Безопасность** - Хеширование паролей, валидация токенов, API ключи

## 🛠️ Технологии

- **Язык**: Go 1.19+
- **База данных**: PostgreSQL
- **Аутентификация**: JWT + Argon2id
- **Миграции**: Нативные SQL миграции

### Предварительные требования

- Go 1.19 или выше
- PostgreSQL 12+

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd httpserver
```

### 2. Настройка базы данных

```bash
psql postgres
# Создание базы данных
createdb chirpy

# Применение миграций
psql $DB_URL -f sql/schema/001_users.sql
psql $DB_URL -f sql/schema/002_add_password.sql
psql $DB_URL -f sql/schema/003_refresh_tokens.sql
psql $DB_URL -f sql/schema/004_chirps.sql
psql $DB_URL -f sql/schema/005_chirpy_red.sql
```
ИЛИ 

```bash
goose postgres "postgres://marat:@localhost:5432/chirpy" up
```

```bash
sqlc generate
```
### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
DB_URL=postgres://username:password@localhost:5432/chirpy?sslmode=disable
PLATFORM=dev
JWT_SECRET=your-super-secret-jwt-key-here
POLKA_KEY=f271c81ff7084ee5b99a5091b42d486e
```

### 4. Запуск сервера

```bash
# Генерация кода SQLC
sqlc generate

# Запуск сервера
go run main.go
```

## 🚀 Примеры использования

### Регистрация и создание chirp

```bash
# 1. Регистрация
curl -X POST http://localhost:8080/api/users \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"mypassword"}'

# 2. Вход
curl -X POST http://localhost:8080/api/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"mypassword"}'

# 3. Создание chirp (используйте access_token из предыдущего ответа)
curl -X POST http://localhost:8080/api/chirps \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"body":"Мой первый chirp!"}'

# 4. Получение всех chirps
curl http://localhost:8080/api/chirps

# 5. Получение chirps конкретного автора
curl "http://localhost:8080/api/chirps?author_id=USER_UUID"
```

## 🔧 Разработка

### Структура проекта

```
httpserver/
├── internal/
│   ├── database/     # Генерируемый SQLC код
│   ├── handlers/     # Обработчики HTTP запросов
│   ├── helpers/      # Вспомогательные функции
│   └── auth/         # Аутентификация и авторизация
├── sql/
│   ├── schema/       # Миграции базы данных
│   └── queries/      # SQL запросы для SQLC
├── assets/           # Статические файлы
└── main.go          # Точка входа
```

### Генерация кода

```bash
# Генерация кода базы данных
sqlc generate

# Запуск тестов
go test ./internal/...
```

### Переменные окружения

| Переменная | Обязательная | Описание |
|------------|--------------|----------|
| `DB_URL` | Да | PostgreSQL connection string |
| `JWT_SECRET` | Да | Секрет для подписи JWT токенов |
| `POLKA_KEY` | Да | API ключ для вебхуков Polka |
| `PLATFORM` | Нет | Режим работы (dev/production) |

## 🐛 Отладка

### Эндпоинты для разработки

```http
GET /api/debug/db - Проверка подключения к базе данных
GET /api/healthz - Проверка здоровья сервера
```

### Логирование

Сервер использует структурированное логирование с эмодзи для лучшей читаемости:

- 🚀 - Запуск сервера
- 🔐 - Операции аутентификации
- 🐦 - Операции с chirps
- ❌ - Ошибки
- ✅ - Успешные операции

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте feature ветку (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

**Chirpy API** - Мощный и безопасный бэкенд для социальной платформы микроблогов 🐦